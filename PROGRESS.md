# 진도 추적

## 현재 상태
- **날짜**: 2026-05-03
- **진도**: 4주차 Day2 일부 완료 (BOJ 19942). 4-B 1285, 4-C 17471 미완
- **복귀일**: 2026-05-03 (오늘) — 옵션 A 감각 회복 모드
- **페이스**: 오늘 30분 감각 회복 → **5/4(월)부터 2시간/일 본격 복귀 (사용자 약속)**
- **시험**: 응시 안 함 → 원래 커리큘럼 속행
- **새 마무리 예상**: 5/15 → **7/13 전후** (누적 +59일)

## 복귀 일정 (2026-05-04부터)
| 날짜 | 할 일 | 비고 |
|------|-------|------|
| 5/3 (일) | 19942 PROGRESS 정독 + 1285 문제 읽기 (30분) | 감각 회복 |
| 5/4 (월) | BOJ 1285 (스위치 켜고 끄기) | Day2 잔여 |
| 5/5 (화, 어린이날) | BOJ 17471 (게리맨더링) | Day2 잔여 |
| 5/6 (수) | BOJ 1987 (알파벳, 비트마스킹 풀이) | Day3 |
| 5/7 (목) | BOJ 14890 (경사로) | Day3 |
| 5/8 (금) | BOJ 1062 (가르침) | Day3 |
| 5/9 (토) | BOJ 1094, 2234 | Day4 |
| 5/10 (일) | BOJ 14391 + **2206 복습** | Day4 + 밀린 복습 |
| 5/11 (월) | BOJ 13244, 5430 | Day5 |
| 5/12 (화) | BOJ 14405, 15353 | Day5 |
| 5/13 (수) | BOJ 15926, 3015 | Day6 |
| 5/14 (목) | 4주차 주간 복습 | Day6 |
| 5/15 ~ 5/30 (16일) | 5주차 그리디/투포인터 (26문제) | 1.5문제/일 |
| 5/31 ~ 6/9 (10일) | 6주차 이분탐색/LIS (15문제) | 1.5문제/일 |
| 6/10 ~ 6/26 (17일) | 7주차 DP (25문제) | 1.5문제/일 |
| 6/27 ~ 7/13 (17일) | 8주차 펜윅/최단거리 (26문제) | 1.5문제/일 |

## 스케줄 슬립 로그
| 발생일 | 기간 | 누적 | 사유 | 만회 계획 |
|--------|------|------|------|-----------|
| 2026-04-14 ~ 04-19 | 5일 공백 | +6일 (노션 재조정) | 손 안 댐 | Day2부터 재개 — 완료 |
| 2026-04-23 ~ 04-26 | 4일 (하루 1시간) | 추가 +4일 | 본가 방문 | 4/27 복귀 후 4-B 1285부터 재개 — **불이행** |
| 2026-04-24 ~ 04-25 | 본가 방문 중 추가 슬립 | 추가 +1일 | 14620이 4/24~25 이틀에 걸침 → 2206 복습 미수행 | 복귀 후 처리 — **불이행** |
| 2026-04-27 ~ 05-03 | **7일 완전 공백 (3차 대형 슬립)** | 추가 +7일 | 본가 방문 후에도 알고리즘에 손 안 댐. 사용자 표현: "본가 방문 이후로 알고리즘 공부 손 안 댔어" | **미정 — 사용자가 결정해서 다음 대화에서 입력 필요** |

**누적 슬립**: 원래 5/15 마무리 예정 → 현재 누적 +18일 → **마무리 6/10 전후**.

**3주(4/14~5/3) 동안 슬립 3회 + 본가 방문 중 미세 슬립 1회.** 패턴이 굳어지고 있음:
- 1차(4/14~19): 손 안 댐 → "Day2부터 재개"로 복구
- 2차(4/23~26): 본가 방문 — "복귀 후 만회" 약속
- **2차 만회 약속(1285·17471·2206 복습) 불이행** → 3차 슬립으로 직결
- 3차(4/27~5/3): 본가 방문 끝났는데도 손 안 댐

## 일일 기록

### 2026-05-03 (복귀 1일차 — 감각 회복)
- 8일 공백 후 복귀. 30분 감각 회복 모드
- 계획: 19942 PROGRESS "막혔던 포인트 6개" 정독 (10분) + 1285 문제 읽고 종이에 비트마스킹 구조 5분 고민 (15분)
- **5/4(월)부터 2시간/일 본격 복귀 약속**
- 노션 일정 재조정: 4주차 Day3~Day6 날짜 갱신 진행 (5~8주차는 사용자 확인 후)

### 2026-04-25 (4/24 작업 이어서)
- 오늘 푼 문제: BOJ 14620 (꽃길) 재풀이 — 백트래킹 복습 (4/24~25 이틀에 걸쳐 완료)
- 본가 방문 중 두 번째 복습일. 4/25 원래 계획(2206 복습) 미수행

#### BOJ 14620 (꽃길) — 재풀이 (백트래킹 can/do/undo 복기)
- **잘한 부분**: can/do/undo 3함수 구조 자체는 손에 붙어 있음 — 4/8 학습 패턴이 살아남. 1D 인덱스 변환(`p/n`, `p%n`)도 자연스럽게 적용. 두 번째 시도에서 7개 카테고리 지적 받고 거의 다 스스로 수정 (출력 추가, `i<4`, 중심 처리, visited 판정, 범위 후 continue, sum 짝 복원, cnt==3 return)
- **반복 실수 (4/8 후 두 번째 발생)**:
  - **canPlant/plant/unPlant에 중심 (y,x) 빠뜨림** — 4방향만 처리. 4/8 PROGRESS의 "막혔던 포인트 ②"와 **완전 동일**. 패턴 인지가 아직 자동화 안 됨
  - **canPlant 최종 코드에서도 중심의 visited 체크 없음** — 논리적으로는 go의 `i+1` 구조 + 4방향 검사로 커버되지만, 의도 명시 차원에서 누락
- **새로 짚인 실수**:
  - `for(int i = 0; i < n; i++)` — `dy/dx` 4방향 배열인데 상한을 `n`으로 씀. n과 4가 시각적으로 비슷해서 놓침. **방향 배열 루프는 항상 `< 4`** 라는 관용구 고정 필요
  - `canPlant`에서 범위 벗어난 후에도 `garden[ny][nx]` 접근 (else/continue 없음) — 첫 시도에서 `if-if` 두 줄로 작성. 범위 체크 후 `continue`/`return` 빠지면 OOB 접근
  - `garden`(비용 배열)으로 점유 판정 시도 → **`visited`(점유 배열)와 역할 분리 명확히**: garden = 정적 입력 데이터, visited = 동적 상태
  - cnt==3 분기에서 `sum = 0` 리셋 시도 → 전역 누적값을 강제 리셋하면 형제 호출 다 망가짐. **plant/unPlant 짝으로 복원**하는 게 정답
- **배운 점 1 (4/8 학습 보강)**: **sum 관리는 두 패턴 다 유효**
  - (A) 매개변수로 `sum + cost` 넘기기 (4/8 PROGRESS "배운 점 2")
  - (B) 전역 sum + plant에서 더하고 unPlant에서 빼기 (이번 풀이)
  - 핵심은 "변경한 상태는 반드시 복원" 원칙. (A)는 자동 복원, (B)는 명시 복원. 어느 쪽이든 OK
- **배운 점 2**: **canPlant의 중심 체크 생략이 논리적으로 안전한 이유** — go가 `i+1`부터 순회하므로 같은 p 두 번 방문 불가. 새 중심이 기존 꽃의 영역과 겹친다면, 기존 꽃의 중심 위치는 새 꽃의 4방향 중 하나로 잡혀서 visited=1로 걸림. → 그래도 의도 명시 차원에선 중심 체크 추가 권장
- **배운 점 3**: **방어적 코드 관용구 2개 고정**
  - 방향 배열 루프: 항상 `for(int i = 0; i < 4; i++)` (또는 `< 8`)
  - 범위 체크 직후: `continue` / `return` 명시 (절대 다음 줄에 배열 접근 두지 말기)
- **느낀 점**: 패턴(can/do/undo)은 손에 붙었지만 **중심 좌표 빠뜨리기**가 또 나옴 — 4/8 직후엔 안 잊는데 17일 지나니 같은 자리에서 또 넘어짐. **"꽃 = 중심+4방향 = 5칸"이라는 그림을 코드 짜기 전 종이에 먼저 그리는 절차** 필요. 본가 방문 중 첫 풀이로는 시간 초과됐지만, 코드 리뷰 통한 자기 수정 능력은 살아 있음 확인


- 오늘 푼 문제: BOJ 19942 (다이어트) — 4주차 Day2, 비트마스킹 첫 문제
- 오늘 학습: 비트마스킹을 "부분집합 전체 순회" 관점으로 전환 + vector 사전순 비교 idiom

#### BOJ 19942 (다이어트) — 비트마스킹 부분집합 순회 + 사전순 tiebreak
- **잘한 부분**: 문제 보자마자 "조합으로 풀 수 있는데 왜 비트마스킹 주차?"라는 메타 질문을 스스로 던짐 — 비트마스킹을 **새 알고리즘이 아니라 "같은 부분집합 탐색의 다른 표현"**으로 이해한 신호. `for(i=0; i<(1<<n); i++)` 전체 순회 구조도 막힘 없이 작성. 최종적으로 0-인덱스 비트 + 1-인덱스 배열 매핑(`idx = j+1`)으로 깔끔하게 정리
- **막혔던 포인트**:
  ① **조건 방향 뒤집힘**: 처음에 `mp >= sumP` (기준치 ≥ 섭취량)으로 썼는데 문제는 **섭취량 ≥ 기준치**여야 함. "기준치 만족"이란 자연어를 부등호로 옮길 때 방향 헷갈림
  ② **sum 초기화 함정**: `int sumP, sumF, sumS, sumV, sumC = 0;` — C++에선 **마지막 변수에만 0 대입**. 나머지는 쓰레기값. 다섯 개 다 따로 초기화 필요
  ③ **비트 인덱싱 불일치 (가장 치명적)**: i 루프는 `< (1 << n)`으로 비트 0~n-1 사용, 내부 j 루프는 `1 << j` with `j=1..n`으로 비트 1~n 검사 → **n=15일 때 식재료 15번이 영원히 후보에서 빠짐**. 11723에서 썼던 `1 << idx` 관례와 이 문제 루프 상한이 안 맞아서 생긴 실수
  ④ **사전순 비교를 마스크 정수끼리 하려고 함**: `currN[0] < minN[0]` 식으로 i 값 비교 → 반례 존재 (`0b1001={1,4}`(i=9) vs `0b0110={2,3}`(i=6): 사전순은 `{1,4}`가 빠른데 i는 6이 작음). **사전순은 인덱스 리스트 자체끼리** 비교해야 함
  ⑤ **vector 메서드 오용**: `currN.pop()` — vector엔 `pop()` 없음, `pop_back()`
  ⑥ `cin.tie(null)` — C++엔 `null` 없음, `NULL`/`nullptr`
- **배운 점 1 (가장 중요)**: **비트마스킹 = 부분집합 탐색의 간결한 표현**. 조합 백트래킹과 본질 같지만 `for(i=0; i<(1<<n); i++)` 한 줄로 모든 부분집합 순회 + 상태가 정수 하나 → 저장/비교/전달 단순. N ≤ 20 정도가 마지노선
- **배운 점 2**: **vector의 `<` 연산자는 자동 사전순 비교**. `if(curr < best)` 한 줄이면 끝. 사전순 tiebreak 관용구:
  ```cpp
  if (sumC < minC || (sumC == minC && currN < minN)) { minC = sumC; minN = currN; }
  ```
  minN이 비어있을 때도 `sumC < minC`가 먼저 참이 돼서 단락평가로 안전
- **배운 점 3**: **0-인덱스 비트 + 1-인덱스 배열 매핑 idiom** — `for(j=0; j<n; j++) if(i & (1<<j)) { int idx = j+1; ... }`. 비트는 0부터 관리하면 루프 상한 `(1<<n)`이 자연스럽고, 배열만 `j+1`로 매핑. 11723의 `1<<idx` 관례를 이 문제에 그대로 끌어오려다 상한 안 맞아서 버그 난 교훈
- **배운 점 4**: **여러 변수 초기화는 한 줄로 묶지 말 것**. `int a, b, c = 0;`는 c만 0. 안전하게 한 줄씩 또는 `int a=0, b=0, c=0;`
- **느낀 점**: "이게 왜 비트마스킹 주차에 있지?"라는 질문을 먼저 던진 게 좋았음. 도구를 **어떤 문제 유형에 왜 쓰는지**부터 정리하고 들어가면 같은 문제에서도 학습량이 달라짐. 5일 공백 복귀일이지만 "집합=정수 하나" 감각이 11723 때보다 한 단계 더 구체화됨 — 순회 단위(`for(i=0; i<(1<<n); i++)`)까지 손에 붙기 시작

### 2026-04-14
- 오늘 푼 문제: BOJ 11723 (집합) — 4주차 비트마스킹 Day1
- 오늘 학습: 비트마스킹 개념 (개념교안 p107~130)

#### BOJ 11723 (집합) — 비트마스킹 기본 연산 6종
- **잘한 부분**: add/remove/check/toggle/all/empty 여섯 연산의 비트 연산 매핑을 스스로 도출 (`|=`, `& ~`, `&`, `^`, `(1<<21)-1`, `0`). 코드 전체 구조 깔끔하게 작성. `1 << (idx-1)` 대신 `1 << idx`로 통일하는 관례 선택도 스스로 판단
- **막혔던 포인트**: ① 처음에 add를 배열 인덱싱(`a[2 << n]`)으로 접근 — "집합=정수 하나"가 아닌 "집합=크기 20 배열" 사고에 갇혀있었음. 비트마스킹의 본질("집합 전체를 int 한 개로 표현")을 체감 후 수정 ② add 함수에 중복 체크(`if(s & (1<<idx)) return;`)를 넣음 — `|=`가 멱등(idempotent)이라 불필요 ③ `ios_base::sync_with_stdio(false); cin.tie(NULL);` 누락으로 TLE (M ≤ 3,000,000 입력 처리)
- **배운 점 1 (가장 중요)**: **"집합 = 정수 하나"** — 비트마스킹은 배열 대체가 아니라 **사고 단위 전환**. 20개 원소의 집합 상태 전체가 `int s` 한 개 안에 들어감. 이 감각 없이 배열로 접근하면 `all`·`empty`·합집합·교집합이 O(N) 루프가 되지만, 비트마스킹이면 O(1) 단일 연산
- **배운 점 2**: **AND vs XOR의 의미 차이** — `&`는 "정보 추출"(읽기), `^`는 "상태 변경"(쓰기/토글). check에 XOR 쓰면 기술적으론 가능하지만 의도가 흐려져 코드 읽는 사람이 "토글"로 오해. **비트 연산은 관용구** — 의도-연산자 매핑을 익혀야 함
- **배운 점 3**: **`|=`의 멱등성** — 이미 켜진 비트에 `|=`해도 결과 동일. add에 중복 체크 불필요. 한 줄로 `s |= (1<<x)` 끝
- **배운 점 4 (C++ 코테 국룰)**: **`ios_base::sync_with_stdio(false); cin.tie(NULL);`** 2줄은 main 시작 템플릿으로 고정. 입력 많은 문제에서 TLE 방지. 단, `scanf/printf`와 섞어쓰기 금지, `endl` 대신 `'\n'` 사용
- **배운 점 5 (입출력 함정)**: **`cin >>` + `getline` 섞을 땐 `cin.ignore()` 필수** — `cin >>`가 버퍼에 남긴 `\n`을 getline이 빈 줄로 읽어버림. 공백 포함 문자열은 반드시 getline 사용
- **느낀 점**: "배열로 자연스럽게 생각하는 바람에 의문이 들었다" — 사고 전환이 비트마스킹 학습의 진짜 난관. 연산 자체는 간단하지만 **"집합을 정수 하나로 보는" 관점**이 안 잡히면 코드는 돌아가도 학습 목적은 0%. 4주차 초반 지나면서 이 감각 계속 단련 필요

### 2026-04-13
- 오늘 푼 문제: BOJ 2206 (벽 부수고 이동하기)

#### BOJ 1525 (퍼즐) — 상태공간 BFS + map
- **잘한 부분**: map과 string 상태 표현을 처음 배운 후 1시간 내에 BFS 구조 완성. 2D↔1D 변환(pos/3, pos%3)을 자연스럽게 적용. swap→탐색→swap 복원 백트래킹 패턴을 BFS 안에서 정확히 구현. visited 거리 누적, 도달 불가 -1 처리까지 직접 설계
- **막혔던 포인트**: ① 처음에 전역 2D 배열로 접근 → BFS는 여러 상태가 큐에 동시에 존재하므로 전역 배열 하나로는 상태 관리 불가능 ② 목표 문자열 "1234567890"(10글자)으로 오타 → "123456780"(9글자)이어야 함 ③ swap 복원을 continue 경로에서만 했다가 push 후에도 필요하다는 것 인지
- **배운 점 1**: **상태가 크면 map<string, int>으로 visited 대체** — 배열 인덱스로 표현 불가능한 상태(퍼즐 배치 등)는 문자열로 직렬화해서 map의 키로 사용
- **배운 점 2**: **BFS에서 전역 상태 사용 불가** — 큐에 여러 상태가 쌓이므로, 각 상태를 독립적으로 들고 다녀야 함 (string을 큐에 넣기)
- **배운 점 3**: 3주차에서 배운 패턴들(2D↔1D 변환, swap 복원, BFS visited 누적)이 새로운 유형에서도 그대로 적용됨을 확인

#### BOJ 15686 (치킨 배달) — 백트래킹 + 시뮬레이션
- **잘한 부분**: 치킨집 조합 → 거리 계산 → 최솟값 갱신 구조를 직접 설계. combi 기저 조건에서 바로 거리 계산하는 깔끔한 구조로 스스로 전환. push_back/pop_back 백트래킹 패턴 자연스럽게 적용
- **막혔던 포인트**: ① cList를 지역변수로 선언해서 재귀 간 공유 안 됨 ② `for`와 `if` 혼동 (기저 조건) ③ 루프 변수 `i` 대신 `start + 1` 넘기는 실수 (14620, 15684에 이어 세 번째) ④ 꺾쇠 `>>>` 개수 부족 ⑤ `.first()` 함수 호출로 작성 (멤버 변수인데)
- **배운 점 1**: **조합 생성 + 즉시 평가** — chickenList에 저장 후 순회보다, combi 기저에서 바로 계산이 깔끔. 14620에서 배운 "백트래킹 안에서 바로 처리" 패턴 재확인
- **배운 점 2**: **전역 벡터 + push/pop = 백트래킹 상태 관리** — visited 배열의 마킹/복원과 동일한 원리가 벡터에서도 적용됨

#### BOJ 2206 (벽 부수고 이동하기) — BFS 상태확장
- **잘한 부분**: 17071에서 배운 visited 차원 확장을 스스로 적용. `visited[2][N][N]`으로 "벽 부쉈냐/안 부쉈냐" 상태 분리하는 구조를 직접 설계. 큐에 `{cnt, y, x}` 튜플로 상태를 넘기는 것도 직접 구현
- **막혔던 포인트**: ① 튜플 push 순서 `{y,x,cnt}`와 tie 순서 `{cnt,y,x}` 불일치 ② `==` 대신 `=` 대입 실수 (15684에 이어 반복) ③ `visited[cnt][ny][nx] + 1` — 현재 위치가 아닌 다음 위치에서 거리를 읽음 ④ 출력 로직에서 -1 조건을 "둘 다 0"이 아닌 "하나만 0"으로 잘못 작성 ⑤ if/else if 연결 없이 독립 if문 3개로 다중 출력
- **배운 점 1**: **visited 차원 확장 패턴** — 같은 좌표라도 "상태가 다르면 다른 노드"로 취급. `visited[상태][y][x]`로 분리하면 BFS의 전역 visited 공유 문제 해결
- **배운 점 2**: **조건 분기 깔끔하게** — `if(벽 && 이미 부쉈으면) continue; int ncnt = cnt; if(벽) ncnt = 1;`으로 하면 cnt==0/1 분기를 따로 안 나눠도 됨
- **배운 점 3**: **차원 확장은 추가 상태 가짓수가 작을 때 유효** (2, K+1, 64 등). 상태가 너무 크면 map으로 전환 (→ BOJ 1525)

### 2026-04-09
- 오늘 푼 문제: BOJ 15684 (사다리 조작) — 3주차 최종 문제

#### BOJ 15684 (사다리 조작) — 시뮬레이션 + 백트래킹
- **잘한 부분**: 문제 보자마자 "조합으로 0/1/2/3개 시도" 아이디어 스스로 도출. `check / draw / undraw / go` 4함수 구조를 힌트 없이 설계. 2D→1D 인덱스 변환(14620에 이어 두 번째 적용) 직접 시도. "시간 오래 걸릴 것 같다"는 감도 맞음 — 단순 조합 나열의 복잡도 문제를 스스로 감지
- **막혔던 포인트**: ① **사다리타기 → 백트래킹 연결**이 쉽지 않았음. "최적화 문제"로 포장돼 있어 DP/그리디 의심 → 답 상한이 3으로 작다는 점이 백트래킹 단서임을 나중에 이해 ② **문제 규칙을 배열로 모델링**이 한 겹 더 어려움: "가로선 = 두 세로선 사이 공간"이라는 비직관적 정의를 스스로 만들어야 함 ③ **이동 규칙이 4방향도 앞뒤도 아님** — "행은 무조건 내려가고, 가로선 유무에 따라 조건부로 좌/우 한 칸" 패턴이 새로움. check에서 "왼쪽 이동은 `board[r][x-1]`, 오른쪽 이동은 `board[r][x]`" 비대칭 접근이 헷갈림 ④ **열 범위가 n이 아닌 n-1**인 이유를 "최적화"로 오해 → 실제론 정의에서 자동 파생되는 결과 ⑤ **인덱스 순서 불일치 재발**: main에서 `board[행][열]`로 넣고 check에서 `board[열][행]`으로 읽음 (14620과 동일 패턴 실수) ⑥ **for 루프 변수 실수 재발**: `int y = idx / n`에서 `idx`가 아닌 `i`를 써야 함 (14620과 완전 동일) ⑦ check 종료조건 반대 (`==`와 `!=` 혼동) ⑧ 내부 루프 상한 `m`(가로선 개수)과 `h`(높이) 혼동 ⑨ "인접 가로선 금지" 조건(`board[y][x-1]==0 && board[y][x+1]==0`)을 떠올리지 못함 ⑩ 답 저장 전역 변수 없이 그냥 return
- **배운 점 1**: **답 상한이 작은(≤10) 최적화 문제 = 백트래킹 신호**. "최소 몇 개?" 문제에서 답 상한이 명시적으로 작으면 DFS/백트래킹으로 k=0,1,2,...,max 순회하면서 성공 즉시 리턴하는 구조가 정석
- **배운 점 2**: **문제의 자연어 → 자료구조 정의는 별도의 사고 단계**. "가로선은 두 세로선 사이의 연결이다" → "board[r][c] = r행에서 c번과 c+1번을 잇는다" → "그래서 c 범위는 1~n-1이다" 순서로 **정의부터 확정**하고 코드로 내려가야 함. 정의 없이 코드부터 쓰면 범위/인덱스 실수가 연쇄적으로 발생
- **배운 점 3**: **2D→1D 인덱스 변환 공식** `idx = r*W + c`, `r = idx/W`, `c = idx%W`. **W는 "한 행의 가로 길이"** (이 문제에선 n-1). 외우지 말고 그림 그려서 유도할 수 있어야 함
- **배운 점 4**: **백트래킹 가지치기 필수**: `if(cnt >= answer) return` 가지치기가 없으면 시간초과 가능. 최적화 문제에서 "이미 더 좋은 답을 찾았으면 즉시 포기"는 백트래킹의 기본 에티켓
- **배운 점 5**: **check 함수는 부작용 없는 검사만, go는 상태 변경과 탐색만**. 역할 분리가 명확할수록 디버깅이 쉬워짐. 14620의 can/do/undo 패턴이 여기서도 그대로 재현됨
- **반복된 실수 (주의 필요)**:
  - **"배열 인덱스 순서 불일치"**: 14620에서도, 15684에서도 나옴. → 앞으로는 **배열 선언 옆에 주석** `// board[행][열] = 의미` 강제하기
  - **"for 루프 변수 vs 함수 파라미터 혼동"**: 14620의 `start/i` 버그와 15684의 `idx/i` 버그가 완전 동일. → **"루프 안에서 파라미터로 계산 금지, 반드시 `i`로"** 규칙 정착 필요
- **느낀 점**: "사다리타기를 배열의 형태로 구상하는 것, 기존에 네 방향이나 수직선 앞뒤 이동이 아닌 새 이동 패턴, 범위 설정까지 모두 떠올리기 힘들었음". 3주차 최종 보스 체감. 하지만 **풀고 나니 14620·1189에서 배운 백트래킹 뼈대 + 2D→1D 변환 + 시뮬레이션 함수 작성**이 하나의 복합 문제로 통합되는 경험이었음. 4주차 비트마스킹 들어가기 전에 좋은 마무리

### 2026-04-08
- 오늘 푼 문제: BOJ 14620 (꽃길), BOJ 1189 (컴백홈)

#### BOJ 1189 (컴백홈) — DFS 백트래킹 (경로 수 세기)
- **잘한 부분**: 14620에서 익힌 백트래킹 패턴을 빠르게 적용. 4방향 탐색 + visited 마킹/복원, 거리 매개변수 누적, `(0, C-1)` 도착 시 거리 K 일치 여부 체크까지 직접 작성. 한 함수 안에 모두 넣되, 분리할지 여부도 스스로 판단함 ("이건 굳이 안 나눠도 되겠다")
- **막혔던 포인트**: ① 베이스 케이스에서 `dist == K`만 체크하고 `return`을 안 해서 도착 후에도 for문이 실행됨 (정답엔 영향 없지만 낭비) ② 함수 진입 시 `visited[y][x] = 1`을 한 줄 더 써놓고는 unset은 caller가 하는 비대칭 패턴 — visited 처리는 "caller가 마크" 또는 "callee가 마크" 둘 중 하나로 통일해야 함
- **배운 점 1**: **visited 마킹의 두 가지 idiom** — (A) caller가 자식 마킹: 진입 전 `visited[ny][nx]=1` → 재귀 → `visited[ny][nx]=0`, 시작점은 main에서 마킹. (B) callee가 자기 마킹: 함수 첫 줄에서 `visited[y][x]=1` → 재귀 호출 → 함수 끝에서 `visited[y][x]=0`. 두 패턴은 섞으면 안 됨
- **배운 점 2**: **함수 분리 여부 판단 기준** — 14620은 "5칸을 동시에 건드리는" 복잡한 변경이라 canPlant/plant/unplant 분리가 효과적이었지만, 1189는 한 칸만 건드리고 검사도 조건 3개라 굳이 분리 필요 없음. 분리는 "이름을 붙이면 가독성이 올라가는가"가 기준
- **배운 점 3**: 같은 백트래킹 뼈대(상태 변경 → 재귀 → 복원)가 반복되며 점점 손에 익는 중. 14620 직후라 패턴 전이가 빠르게 일어남
- **느낀 점**: 14620에서 함수 분리를 고생해서 익힌 덕에 1189는 "이건 분리 안 해도 된다"는 판단까지 빠르게 도달. 패턴이 점점 자기화되고 있음

#### BOJ 14620 (꽃길) — 백트래킹 + 함수 분리 idiom
- **잘한 부분**: BFS는 visited를 공유 못 한다는 점에서 백트래킹으로 전환을 스스로 판단. canPlant/plant/unplant 세 함수로 분리한 구조도 직접 도출(힌트는 받았지만 빠르게 적용). 시작 위치 매개변수가 "중복 조합 회피"를 위함이란 것도 스스로 답함
- **막혔던 포인트**: ① 처음엔 한 함수 안에서 4칸 표시 도중 실패하면 부분 복원하는 방식 시도 → 시점 너무 헷갈림 ② canPlant/plant/unplant에서 중심 (y,x)를 빼먹음 (4방향만 처리) ③ `int y = start/N` → 루프 변수 `i`로 안 바꿔서 (y,x)가 계속 첫 위치로 고정 ④ `sum += cost` 지역변수 누적 → 형제 호출 사이에 sum 오염 ⑤ 입력 받을 때 `j < N` 대신 `i < N` 오타
- **배운 점 1 (가장 중요)**: **백트래킹 함수 분리 idiom (can/do/undo)** — 한 함수에 다 넣으면 "변경 도중 실패" 시점이 폭발함. canPlant(부작용 없는 검사) → 통과 시에만 plant(변경) → unplant(복원)로 분리하면 "부분 변경 후 복원" 시나리오가 아예 사라짐. 본인 표현으로 "함수 나누니 시점이 명확해지고 가독성이 살아남"
- **배운 점 2**: **두 종류의 "되돌릴 거리" 구분** — 공유 자료구조(used[][])는 명시적 unplant로 복원, 누적 값(sum)은 변경 말고 매개변수로 `sum + cost` 넘기기. 지역변수에 직접 누적하면 형제 호출 사이로 새어나감
- **배운 점 3**: **2D 좌표 시작점 → 1D 인덱스 변환** — `for(p = start; p < N*N; p++)` + `y = p/N, x = p%N`. 격자 백트래킹에서 "다음 위치부터" 처리할 때 1D 변환이 가장 깔끔. 2D로도 `j = (i==sy ? sx : 0)`로 가능하지만 헷갈림 적은 1D 추천
- **느낀 점**: "백트래킹 재귀 함수 구현이 신기하고 재밌다" — 패턴이 손에 잡히기 시작한 신호. BFS/DFS의 "한 함수에 다 넣는" 습관이 백트래킹엔 안 맞다는 걸 체득함

### 2026-04-07
- 오늘 푼 문제: BOJ 9934 (완전 이진 트리)

#### BOJ 9934 (완전 이진 트리) — 분할정복 + 중위순회
- **잘한 부분**: 중위순회의 정의로부터 "가운데가 루트"라는 핵심 통찰을 스스로 잡음. 재귀 구조(depth 줄이며 절반씩 쪼개기)와 기저 사례(`depth==0`) 설계 직접 함. 두 번째 시도에서 left/right 매개변수 도입한 후 `mid = (l+r)/2`로 루트 잡고 재귀 호출하는 구조 완성
- **막혔던 포인트**: ① 첫 시도에서 `go(depth, num)`으로 **크기만** 넘김 — 같은 크기의 왼쪽/오른쪽 서브트리를 구분할 방법이 없다는 걸 못 깨달음. "구간의 위치" 정보가 빠진 것 ② `left`, `right`가 `<bits/stdc++.h>` + `using namespace std;`와 충돌(스트림 조작자) ③ `int idx = ...`, `int mid = ...` 타입 선언 누락 ④ 결과 출력 단계 빼먹음
- **배운 점 1**: **분할정복 재귀의 매개변수는 "이 함수가 보고 있는 구간이 어디인지"를 충분히 표현해야 함**. 크기만으로는 부족하고 (시작, 끝) 또는 (시작, 길이)가 필요. → 함수의 의미를 한 문장으로 정의하는 습관 필요
- **배운 점 2**: 같은 레벨 노드들의 출력 순서 보장 — `pair<int,int>` 모아서 `sort`하면 안정 정렬이 아니라 위험. **레벨별 벡터** (`vector<int> level[20]`)에 바로 push하면 재귀 호출 순서(왼→오) 덕에 자연스럽게 좌→우 순서 보장
- **배운 점 3**: 재귀 호출 순서가 곧 트리 순회 순서 — `level[idx].push_back()` → 왼쪽 재귀 → 오른쪽 재귀 순으로 쓰면 같은 레벨 안에서 좌→우가 자동 보장됨
- **느낀 점**: BFS/DFS는 "틀"이 있지만 분할정복 재귀는 매번 "이 함수의 의미가 뭔지" 새로 정의해야 해서 더 어렵게 느껴짐. 패턴 익숙해지려면 비슷한 문제 더 풀어야 함

### 2026-04-06
- 오늘 푼 문제: BOJ 1987 (알파벳), BOJ 2529 (부등호)

#### BOJ 2529 (부등호) — DFS 백트래킹
- **잘한 부분**: 첫 숫자는 부등호 비교 없이 0~9 모두 가능하므로 main에서 각각 DFS 호출하는 구조를 직접 설계함. DFS 내에서 for(0~9) 탐색하며 부등호 조건 체크, used 배열로 중복 방지, idx로 부등호 인덱스 추적
- **막혔던 포인트**: ① `s + num`으로 int를 바로 문자열에 추가 → `s += ('0' + num)`으로 char 변환 필요 ② 문자열 `s`에 대한 백트래킹(pop_back) 누락 — used만 되돌리고 s는 안 되돌림 ③ `int(s)`로 비교 시도 → 10자리 넘어 int 오버플로, string 비교로 해결 ④ `''` (char) vs `""` (string) 혼동
- **배운 점 1**: 백트래킹 되돌리기는 반드시 **쌍으로** — `used[i]++` ↔ `used[i]--`, `s += ...` ↔ `s.pop_back()`. 변경한 상태는 전부 되돌려야 함
- **배운 점 2**: idx와 depth가 같은 값으로 움직이면 하나로 합칠 수 있음 — 변수 줄이기
- **배운 점 3**: 자릿수가 같은 숫자는 string 사전순 비교 = 숫자 크기 비교 → int 오버플로 걱정 없이 max/min 비교 가능

#### BOJ 1987 (알파벳) — DFS 백트래킹
- **잘한 부분**: DFS 백트래킹 구조(상태 변경 → 재귀 → 상태 복원)를 직접 구현함. 알파벳 배열로 visited 역할을 대체하는 아이디어도 이해
- **막혔던 포인트**: ① BFS로 접근하려 했으나, 경로마다 사용한 알파벳 상태가 달라 BFS로는 상태 관리가 어렵다는 것을 깨달음 ② 기저 사례(return 조건)를 명시적으로 써야 한다고 생각했으나, 갈 곳이 없으면 for문이 실행 안 되어 자연스럽게 종료됨을 이해
- **배운 점 1**: BFS vs DFS 판단 기준 — 최단 거리면 BFS, **모든 경로 탐색 / 최대 경로**면 DFS 백트래킹
- **배운 점 2**: 백트래킹의 핵심은 "되돌리기" — `alpha[...]++` → 재귀 → `alpha[...]--`로 다른 경로 탐색 시 깨끗한 상태 보장
- **배운 점 3**: 알파벳 사용 여부 추적이 곧 visited 역할 → 별도 visited 배열 불필요

### 2026-04-05
- 오늘 푼 문제: BOJ 14497 (주난의 난), BOJ 3197 (백조의 호수), BOJ 17071 리팩토링

#### BOJ 3197 (백조의 호수) — 레벨별 BFS × 2 (물 + 백조)
- **잘한 부분**: 브루트포스(매턴 DFS+BFS) 버전을 먼저 직접 설계함. 레벨별 BFS로 최적화하는 구조(큐 4개: waterQ/water_tempQ, swanQ/swan_tempQ)를 강의 보고 구현
- **막혔던 포인트**: ① 브루트포스 버전에서 for문이 녹인 X를 다시 bfs → 연쇄 녹음 (visited 체크 누락) ② `visited_swan[swanY][swanX] = 1` 대입 대신 읽기만 해서 시작점으로 되돌아와 자기 자신을 발견 ③ `b2x = v[1].second` 오타 반복 ④ 'L' 칸도 물로 취급해야 인접 얼음이 녹는다는 점
- **배운 점 1**: 레벨별 BFS를 여러 대상에 동시 적용 — 물 녹이기와 백조 이동을 각각 큐 2개씩, 총 4개 큐로 관리
- **배운 점 2**: tempQ 패턴 — 현재 턴에서 막힌 셀(X)을 tempQ에 모아두고, 다음 턴에 메인 큐로 교체. visited는 초기화하지 않고 누적
- **배운 점 3**: 브루트포스 먼저 → 최적화 순서가 효과적. 브루트포스에서 "매턴 전체 탐색"의 비효율을 체감한 후 레벨별 BFS의 필요성을 이해함

#### BOJ 14497 (주난의 난) — 레벨별 BFS (비균일 비용)
- **잘한 부분**: 큐에서 pair 꺼내는 tie 문법, 기본 BFS 구조를 직접 작성함
- **막혔던 포인트**: ① `q = next_q`를 안쪽 while 안에 넣어 매 원소마다 큐 교체됨 ② `visited`를 범위 체크 전에 설정 → 배열 밖 접근 ③ `'1'` 칸에 visited 안 해서 중복 삽입 ④ `tie(x,y)` 순서가 `{행,열}`과 불일치 ⑤ 후위감소(`junanY--`) 사용으로 배열 표시 위치와 변수 값 불일치 ⑥ 범위 체크 `> N` → `>= N`
- **배운 점 1**: 레벨별 BFS — 비용이 0과 1로 나뉠 때, `'0'`은 현재 큐에 `'1'`은 next_q에 넣고 2중 while로 라운드를 분리
- **배운 점 2**: `sz = q.size()` 패턴은 한 레벨 안에서 큐 크기가 고정일 때만 사용 가능. 현재 큐에 동적으로 추가되면 큐 2개로 분리해야 함
- **배운 점 3**: 0-1 BFS(deque) 개념 — `'0'`이면 앞에, `'1'`이면 뒤에 넣는 방식. 지금은 큐 2개로 풀었지만 나중에 익히면 더 간결

### 2026-04-04
- 오늘 푼 문제: BOJ 17071 (숨바꼭질 5)

#### BOJ 17071 (숨바꼭질 5) — BFS + 움직이는 목표물
- **잘한 부분**: BFS로 visited 완성 → 별도 for문에서 동생 위치 계산하며 비교하는 2단계 분리 구조를 스스로 이해함. 동생 위치 누적 합 계산도 정확히 구현
- **막혔던 포인트**: ① 1차원 visited로 홀짝 비교만 추가하면 될 줄 알았으나, 최단 시각의 홀짝이 안 맞을 때 반대 홀짝 경로를 놓침 ② visited 인덱스에 위치 홀짝(N%2)을 쓰는 실수 → 시각 홀짝(t%2)이어야 함 ③ 레벨 단위 BFS (sz = q.size()) 패턴을 처음 접함
- **배운 점 1**: visited 배열은 "탐색 결과를 담는 그릇" → 홀짝·방향·열쇠 등 상태를 차원으로 추가해 2차원, 3차원으로 확장 가능
- **배운 점 2**: 도달 시각 ≤ t 이고 홀짝 동일 → 제자리 왕복(+1, -1)으로 시간 맞출 수 있다는 논리
- **배운 점 3**: 레벨 단위 BFS — `sz = q.size()`로 현재 레벨 노드 수를 고정하고, 그만큼만 처리 후 t++ → 시각 추적 가능

### 2026-04-03
- 풀고 있던 문제: BOJ 17071 (숨바꼭질 5) — 구조 설계까지 완료
- 막혔던 포인트: q.push 누락, 범위 체크 순서, BFS 안에서 동생 위치 갱신 시도, for문 누락

### 2026-04-02
- 오늘 푼 문제: BOJ 16637 (괄호 추가하기), BOJ 12851 (숨바꼭질 2), BOJ 13913 (숨바꼭질 4)

#### BOJ 16637 (괄호 추가하기) — 재귀 브루트포스
- **잘한 부분**: 숫자/연산자 분리, calc·solve 구조를 직접 설계함
- **막혔던 포인트**: calc 반환형 void로 선언, ret/cnt 변수를 덮어써서 두 분기가 간섭, 종료 조건에서 null 비교
- **배운 점**: "각 위치에서 선택지 전부 시도 → 다음으로 넘기기" 재귀 브루트포스 패턴. solve 호출 시 값을 직접 넘겨야 분기가 독립적

#### BOJ 13913 (숨바꼭질 4) — BFS + 경로 역추적
- **잘한 부분**: BFS 최단거리 로직을 먼저 단계적으로 구현하고, parent 배열 + 재귀 역추적을 직접 설계함
- **막혔던 포인트**: ① `2*N`(시작점)을 `2*n`(현재 위치)으로 써야 하는 대소문자 실수 ② 종료 조건에서 `visited[mN] == K`(거리값과 좌표 비교)로 잘못 작성 ③ 역추적 방법 자체를 떠올리기 어려웠음 — 경로가 여러 개인데 하나만 저장해도 되는지 고민
- **배운 점**: parent 배열로 "어디서 왔는지"만 기록하면 역추적 가능. 모든 최단 경로를 구해야 하면 parent를 벡터로 바꾸고 재귀 백트래킹으로 조합 탐색 — 괄호 맞추기(재귀 브루트포스)와 같은 구조
- **깨달음**: 재귀 브루트포스 패턴이 BFS 역추적, 백트래킹 등 여러 곳에서 반복적으로 등장함. "선택지 앞에서 하나 고르고 끝까지 가본다"가 핵심 뼈대
- **코드 리팩토링 (강의 참고)**: `here` 변수 + `prev[]` 배열 + `for(int next : {here+1, here-1, 2*here})` 패턴으로 정리. 역추적은 `for(int i = K; i != N; i = prev[i])` 반복문 + reverse

#### BOJ 12851 (숨바꼭질 2) — BFS + 경로 수 세기
- **잘한 부분**: BFS 기본 구조, 3방향 탐색을 dy/dx 스타일로 처리, 같은 레벨 재방문 조건 이해
- **막혔던 포인트**: ret++로 경우의 수를 세려 함 → BFS에서 노드는 한 번만 큐에 들어가므로 경로 수 누락
- **배운 점**: 최단 경로 가짓수는 ways 배열로 별도 추적 (`ways[next] += ways[cur]`). 처음 방문 → 큐 push + ways 설정 / 같은 레벨 재방문 → ways만 누적

### 2026-04-01
- 오늘 푼 문제: ~3-D (BOJ 4179)까지
- 배운 점: 상태공간 BFS 기초

---

## BFS 문제별 핵심 테크닉 정리

| 문제 | 유형 | 핵심 테크닉 |
|------|------|------------|
| BOJ 4179 (불!) | 멀티소스 BFS | 불과 사람 BFS 따로 돌리고, 사람 이동 시 불 도착 시간과 비교 |
| BOJ 12851 (숨바꼭질 2) | BFS + 경로 수 | `ways[]` 배열로 최단 경로 가짓수 누적. 같은 레벨 재방문 시 ways만 더함 |
| BOJ 13913 (숨바꼭질 4) | BFS + 경로 역추적 | `parent[]` 배열로 "어디서 왔는지" 기록 → 재귀로 역추적 |
| BOJ 16637 (괄호 추가하기) | 재귀 브루트포스 | 각 위치에서 "괄호 안 씀 / 씀" 두 선택지 → 재귀로 전부 시도 |
| BOJ 17071 (숨바꼭질 5) | 레벨 BFS + 홀짝 | `visited[위치][홀짝]`, `sz = q.size()`로 시각 추적. 도달 시각 홀짝 같으면 왕복으로 대기 가능 |
| BOJ 14497 (주난의 난) | 레벨 BFS (비균일 비용) | **큐 2개 + while 2중**: `'0'`→현재 큐, `'1'`→next_q. `sz` 방식 불가 (현재 큐에 동적 추가되므로) |
| BOJ 3197 (백조의 호수) | 레벨 BFS × 2 | **큐 4개**: 물(waterQ/water_tempQ) + 백조(swanQ/swan_tempQ). 각각 tempQ에 막힌 셀 모아두고 턴마다 교체. visited는 누적 |
| BOJ 1987 (알파벳) | DFS 백트래킹 | 알파벳 사용 배열로 visited 대체. 상태 변경→재귀→상태 복원. 모든 경로 탐색하며 최대 깊이 갱신 |
| BOJ 2529 (부등호) | DFS 백트래킹 | 첫 숫자 0~9 각각 DFS 호출. for(0~9)에서 부등호 조건 체크 + used 배열. 결과는 string으로 max/min 비교 |
| BOJ 15684 (사다리 조작) | 시뮬레이션 + 백트래킹 | 답 상한이 3으로 작음 → k=0,1,2,3 순회 백트래킹. `board[r][c]=1`은 "c번과 c+1번 연결"로 정의(c: 1~n-1). check는 각 열에서 출발해 행마다 좌/우 조건부 이동. 2D→1D 인덱스로 중복 조합 회피. 가지치기 `cnt>=answer` 필수 |

### 레벨별 BFS 패턴 비교

```
패턴 1: sz 고정 (큐 크기 안 변할 때 — BOJ 17071)
while(!q.empty()) {
    int sz = q.size();
    for(int i = 0; i < sz; i++) { ... }
    level++;
}

패턴 2: 큐 2개 (현재 레벨에서 큐가 늘어날 때 — BOJ 14497)
while(!q.empty()) {
    queue next_q;
    jump++;
    while(!q.empty()) {
        // '0' → q에 push (계속 퍼짐)
        // '1' → next_q에 push (다음 라운드)
    }
    q = next_q;
}

패턴 3: tempQ 교체 (큐를 턴 간 유지 — BOJ 3197)
while(true) {
    while(!q.empty()) {
        // 통과 가능 → q에 push
        // 막힘(X) → tempQ에 push
    }
    q = tempQ; clear(tempQ);
    // visited는 초기화 안 함 (누적)
}

패턴 4: 0-1 BFS (deque — 나중에 배울 것)
// '0' → deque 앞에, '1' → deque 뒤에
```

<!-- 매일 아래 형식으로 추가 -->
<!--
### 2026-04-XX
- 오늘 푼 문제:
- 막혔던 포인트:
- 배운 점:
-->
